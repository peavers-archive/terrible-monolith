spring:
  application:
    name: terrible-api-service

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    export:
      prometheus:
        enabled: true
  endpoint:
    prometheus:
      enabled: true
    metrics:
      enabled: true

search:
  host: "localhost"
  port: 9200
  scheme: "http"
  
---
spring:
  profiles: production
  data:
    mongodb:
      host: terrible-mongo
  webflux:
    base-path: /api

search:
  host: "terrible-search"
  port: 9200
  scheme: "http"
