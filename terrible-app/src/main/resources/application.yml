spring:
  application:
    name: terrible-api-service
  batch:
    job:
      enabled: false

server:
  port: 8081

terrible:
  index: media
  thumbnailJob: true
  directoryJob: true

batch:
  delay: 900000 #milliseconds

management:
  endpoints:
    web:
      exposure:
        include: "*"
  metrics:
    export:
      prometheus:
        enabled: true
  endpoint:
    prometheus:
      enabled: true
    metrics:
      enabled: true
